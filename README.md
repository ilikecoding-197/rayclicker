# rayclicker
basic clicker with raylib for fun

# Building
Clone the repo, and generate the build files:
```bash
mkdir build
cd build
cmake ..
```

Then, build:
```bash
make
```

After it builds, a "rayclicker" executable should be in the build folder.
